# baseURL = 'apdegssi.github.io/'
baseURL = 'https://apde.gssi.it/'
locale = 'en-us'
title = 'Analysis and PDE at GSSI'
theme = 'two-line-theme'
buildFuture = true

[markup.goldmark.renderer]
unsafe = true
